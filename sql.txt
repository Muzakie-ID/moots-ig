-- Buat database terlebih dahulu
CREATE DATABASE IF NOT EXISTS mots_ig;
USE mots_ig;

-- Buat tabel members
CREATE TABLE IF NOT EXISTS members (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL,
    foto VARCHAR(255) DEFAULT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Tambah kolom foto dan foto_uploaded_at jika tabel sudah ada (untuk update)
-- ALTER TABLE members ADD COLUMN foto VARCHAR(255) DEFAULT NULL AFTER username;
-- ALTER TABLE members ADD COLUMN foto_uploaded_at TIMESTAMP NULL AFTER foto;
